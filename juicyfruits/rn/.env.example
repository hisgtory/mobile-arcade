# Juicy Fruits API base URL.
#
# Expo의 EXPO_PUBLIC_* 변수는 빌드 타임에 번들에 임베드됨 (런타임 주입 X).
# 실제 값은 .env 파일(gitignored)에 설정. 이 파일은 템플릿.
#
# Local dev: 아래 production URL 그대로 써도 되고,
# 백엔드를 로컬에서 띄울 때는 http://localhost:8080 등으로 덮어써도 됨.
EXPO_PUBLIC_API_BASE=https://arcade-api.hisgtory.com
