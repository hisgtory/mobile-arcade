export { createGame, destroyGame, getPlayScene } from './game';
export { getStageConfig, TOTAL_STAGES } from './logic/stage';
export type { GameConfig, StageConfig, ProductType, CustomerOrder, GamePhase } from './types';
