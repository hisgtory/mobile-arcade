export { createGame, destroyGame } from './game';
export { getStageConfig } from './logic/stage';
export type { GameConfig, StageConfig, PieceShape } from './types';
export { GamePhase, GRID_SIZE } from './types';
