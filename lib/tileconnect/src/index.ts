export { createGame, destroyGame } from './game';
export type { GameConfig, StageConfig, TileData, PathPoint } from './types';
export { GamePhase } from './types';
export { getStageConfig, getMaxStage } from './logic/stage';
